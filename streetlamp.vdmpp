class StreetLamp

instance variables
    private on : bool := false;
    sensor : Sensor;

operations
    public On : () ==> ()
    On() == on := true;

    public Off : () ==> ()
    Off() == on := false;

end StreetLamp